syntax = "proto3";

package recc.proto.daemon;

service DaemonApi {
    rpc Heartbeat (Pit) returns (Pat) {}
    rpc Packet (PacketQ) returns (PacketA) {}
    rpc Pickling (PacketQ) returns (PacketA) {}
}

message Pit {
    float delay = 1;
}

message Pat {
    bool ok = 1;
}

message PacketQ {
    int32 method = 1;
    map<string, string> headers = 2;
    optional bytes content = 3;
}

message PacketA {
    int32 code = 1;
    map<string, string> headers = 2;
    optional bytes content = 3;
}
