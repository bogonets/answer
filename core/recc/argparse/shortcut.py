# -*- coding: utf-8 -*-

from enum import Enum


class Shortcut(Enum):
    b = "b"  # http-Bind
    c = "c"  # Config
    d = "d"  # Developer (Debug)
    h = "h"  # Help (Don't use this flag)
    k = "k"  # signature Key
    p = "p"  # http-Port
    v = "v"  # Verbose
