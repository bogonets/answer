.. meta::
	:keywords: CV2

.. role:: raw-html(raw)
	:format: html

cv2_videocapture_mp
=============================

비디오 파일, 이미지 시퀀스 또는 카메라에서 비디오를 캡처한다.

출력 슬롯
---------

* **frame**

속성 목록
---------

* **Filename**

+-----------------+-------+----------+---------------+
| Rule            + Type  + Required + Default value |
+=================+=======+==========+===============+
| initialize_only + str   + True     +               |
+-----------------+-------+----------+---------------+

읽어올 파일 이름.

* **API Preference**

+-----------------+-------+----------+---------------+
| Rule            + Type  + Required + Default value |
+=================+=======+==========+===============+
| initialize_only + str   + True     + default       |
+-----------------+-------+----------+---------------+

사용할 백엔드 API.

* **다음 반복을 위한 대기시간.**

+-----------------+-------+----------+---------------+
| Rule            + Type  + Required + Default value |
+=================+=======+==========+===============+
| initialize_only + int   + True     + 0             |
+-----------------+-------+----------+---------------+

프레임을 읽은 후 잠시 대기한다. (밀리초)

* **재연결 플래그**

+-----------------+-------+----------+---------------+
| Rule            + Type  + Required + Default value |
+=================+=======+==========+===============+
| initialize_only + bool  + False    + True          |
+-----------------+-------+----------+---------------+

비디오 프레임 획득에 실패하면 재연결한다.

* **Max queue size**

+-----------------+-------+----------+---------------+
| Rule            + Type  + Required + Default value |
+=================+=======+==========+===============+
| initialize_only + int   + True     + 4             |
+-----------------+-------+----------+---------------+

대기열에 넣을 수있는 항목 수의 상한 값.

* **종료 타임아웃**

+-----------------+-------+----------+---------------+
| Rule            + Type  + Required + Default value |
+=================+=======+==========+===============+
| initialize_only + float + True     + 4.0           |
+-----------------+-------+----------+---------------+

람다 파괴시 최대 대기 시간. (초)

